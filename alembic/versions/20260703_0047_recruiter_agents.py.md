# 20260703_0047_recruiter_agents.py

Automation D (recruiters): creates `recruiter_saved_searches` (recruiter-scoped
criteria + last_run_at watermark) and adds the `screening_questions` JSON
column to `job_offers`. Column-only FKs (SQLite-compatible).
