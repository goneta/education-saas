# 20260703_0046_fee_reminders.py

## Purpose
- Creates `fee_reminders` (fee_id, school_id, student_id, level, outstanding_amount, channels JSON) for the reminder automation's anti-spam/escalation tracking.
