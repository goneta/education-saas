# 20260630_0045_payroll.py
## Purpose
- Adds `salary_profiles` (per-employee compensation config) + nullable breakdown columns on `payroll_records` (period_type, pay_type, currency, base/allowances/bonus/overtime/advances/other deductions, social_contributions, tax, payment_method/reference, academic_year_id). All nullable — legacy /operations payroll keeps working.
## Verification
- `python -m alembic upgrade head`; `python -m pytest backend/test_payroll.py`
