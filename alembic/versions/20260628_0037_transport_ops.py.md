# 20260628_0037_transport_ops.py

## Purpose

- Creates `transport_vehicle_positions` (GPS samples), `transport_boarding_events` (boarding attendance), `transport_incidents`, and `transport_fuel_logs` — all school-scoped.

## Local Contracts

- FKs to vehicles/routes/stops/student_profiles/schools. Additive. Positions are the REST data layer of the GPS service (real-time push is roadmap).

## Verification

- `python -m alembic upgrade head`
- `python -m pytest backend/test_transport.py`
