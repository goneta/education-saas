# test_levels.py
## Purpose
- #3 referential: super-admin CRUD + open read (school-admin reads, cannot write 403), duplicate code 409, delete blocked when used by a class / allowed when unused.
## Verification
- `python -m pytest backend/test_levels.py`
