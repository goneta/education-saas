# test_academics.py

## Purpose

- Verifies Slice 3 GPA: weighted computation (16/20 w1 + 12/20 w3 → 65% / 13.0 / 2.6), term scoping, cross-school read rejection (404), and zero on no grades.

## Verification

- `python -m pytest backend/test_academics.py`
