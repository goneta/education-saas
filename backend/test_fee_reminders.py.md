# test_fee_reminders.py

## Purpose
- Levels/channels/escalation (L1 sms+notif, L2 notif, L3 + admin escalation), idempotent rerun (cooldown), paid/future skipped, level escalation resends after cooldown, tenant scoping, endpoint RBAC 403.
