# TeducAI backend dependencies — VERSIONS PINNED (audit CFG-02).
#
# Nothing was pinned before, so CI could validate one set of versions while
# production installed another, and an upstream release could break production
# without a single line of our code changing. These are the versions the test
# suite is green with (337 tests).
#
# INSTALL ORDER MATTERS for the AI agents platform:
#   pip install -r backend/requirements.txt
#   pip install --no-deps openai-agents==0.18.2
# `openai-agents` pins transitive mcp/starlette/pydantic ranges that clash with
# the FastAPI 0.104 stack; installed with --no-deps it runs fine on top of
# openai + pydantic + griffe, which are listed here.
#
# Removed in the same audit (ARCH-04): `motor` (MongoDB) and `langchain` were
# declared but imported nowhere — pure install time and attack surface.

fastapi==0.104.1
uvicorn[standard]==0.51.0
sqlalchemy==2.0.23
alembic==1.12.1
psycopg2-binary==2.9.9
python-dotenv==1.0.0

# Auth / security
passlib[argon2]==1.7.4
argon2-cffi==25.1.0
python-jose[cryptography]==3.3.0
python-multipart==0.0.32
email-validator==2.1.0

# AI
openai==2.45.0
pydantic==2.11.10
griffe==1.5.1
# openai-agents==0.18.2  -> install separately with --no-deps (see header)

# Documents
reportlab==4.0.7
qrcode[pil]==7.4.2
pillow==10.1.0
pypdf==3.17.1
openpyxl==3.1.2

# Infrastructure
redis==5.0.1
boto3==1.34.34
httpx==0.27.2

# Tests
pytest==7.4.3
