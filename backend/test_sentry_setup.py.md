# Tests Sentry

- Confirme le mode désactivé sans DSN, les options de confidentialité, les bornes de taux et le nettoyage des événements, traces et logs.
- L'initialisation est simulée ; aucun test n'envoie de données à Sentry.
- Exécuter `python -m pytest backend/test_sentry_setup.py`.
