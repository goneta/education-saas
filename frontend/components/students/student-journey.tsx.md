# Purpose

Displays the global student number, chronological enrollment journey, concurrent enrollments, read-only academic-year status, local financial visibility, transfer history, and multi-format exports.

# Contracts

- Financial summaries are rendered only when returned by the backend.
- Historical contexts marked read-only cannot expose editing actions.
- Exports are requested from the audited lifecycle API.
