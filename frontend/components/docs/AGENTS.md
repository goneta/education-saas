# components/docs

Reusable, self-contained components for the **TeducAI Platform Docs** site (a public docs experience modeled on Claude's Platform Docs). Content lives in `frontend/lib/docs/content.ts`; these components only render it. No backend calls. Linked from the marketing nav ('Documentation') and routed under `app/[locale]/docs/[[...slug]]`.
