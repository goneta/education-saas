# doc-blocks.tsx

## Purpose
- `DocBlock` union + renderer (`DocBlocks`) and the inline parser (`Inline`: code spans, bold, locale-prefixed links). `slugify` generates heading anchors used by the TOC.
