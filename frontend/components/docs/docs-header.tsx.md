# docs-header.tsx

## Purpose
- Sticky docs top bar: logo + wordmark, top tabs (Guides/Features/Admin/Resources), utility links and the Console button.
- The Resources tab carries a dropdown chevron per the docs-site spec.
- The language selector is a working dropdown (fr/en/es/sw): swaps the locale segment of the current path and navigates; closes on outside click; label reflects the active locale.
