# confirmation-dialog.tsx

## Purpose

- Provides the reusable TeducAI confirmation modal for destructive or sensitive actions.
- Replaces browser `alert()` and `confirm()` prompts with accessible, themed UI and loading protection.

## Verification

- `cmd.exe /c "cd frontend&& npx eslint components/ui/confirmation-dialog.tsx"`
